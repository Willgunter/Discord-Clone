const express = require("express");
const router = express.Router();

// next step to connecting frontend + backend (should be last)

// uh... see GitHub of video for more details

export default router;